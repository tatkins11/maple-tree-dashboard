import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Placeholder until the Netlify site (and later a custom domain) is claimed.
  site: 'https://mapletree-softball.netlify.app',
});
